# DSH 用户画像记忆插件 · 施工清单

> **目标**：让 DSH 知道用户的分领域知识水平与完整画像，在对话中自动用对方听得懂的话交流，提高对话效率与学习效率。
> **形态**：给 DSH 开发一个独立记忆模块（Cordis 插件）。
> **状态**：架构定稿，待施工。

---

## 一、定稿架构

| 面 | 定了什么 |
|---|---|
| **形态** | 独立模块，与 agent 双向互通（读画像 / 写更新）；对外只一个权限门 ＋ 一组读写工具 |
| **数据模型** | 多边形；固定 WBS 清单（七面 × 子板块），穷尽覆盖、允许空白。**落库方式：混着来** = 每面用 `facet` 标签挂条目（轻）＋ 分领域知识水平单开 profile 表（结构化） |
| **颗粒度** | 周级、能力层概括（非日流水、非知识点细节） |
| **存储** | SQLite 为主（FTS5 检索，用 trigram 修中文）；Markdown 可选镜像；后端留可插拔口 |
| **采集** | 双通道。问卷（覆盖 U−S，**用户主动激发 skill**）＋ 隐性观察（覆盖全部，**后台离线分析历史对话**） |
| **S 判据** | 自陈不可靠 ∪ 主观性强 |
| **更新** | 以当次为准覆盖旧记忆；**soft delete 留痕**（active → superseded），可回滚 |
| **仲裁** | **观察优先于问卷** |
| **注入** | 单套：按需注入为主 ＋ 开场限流预热；按内容分路（普遍相关预热 / 情境相关按需） |
| **表达约束** | 分领域水平 → 四档映射 → 拼注入文本（见第四节）。**会话内冻结**：新会话开始算一次，会话中不变 |
| **知识领域刻度** | 固定清单，8 大类 / 31 子领域，见 `docs/知识领域清单.md` |
| **权重** | agent 按任务自动分配；默认全自动，设置里可切半自动；无全手动 |
| **注入量** | 由内容繁荣度自动决定（膨胀问题后续处理） |
| **纪律** | 稳定内容前置、限流、写入留门；策略集中写一处，不做开关、不做界面、不做实验 |
| **维护** | Dream（备忘录，暂不做） |

---

## 二、多边形（七面 × 子板块）

| 顶层 | 子板块 | 通道 |
|---|---|---|
| **I · 躯体** | 生理与健康（体质、体能、疾病） | 🔵 |
| | 睡眠与饮食节律 | 🔵 |
| | 感官与身体条件 | 🔵 |
| **II · 心智** | 认知能力（智力、记忆、注意） | 🔵 |
| | 知识与学科水平 | 🔵 |
| | 思维方式与思辨 | 🟠 |
| | 人格特质 | 🟠 |
| | 情绪模式与心理强度 | 🟠 |
| **III · 价值与意愿** | 三观与信仰 | 🔵 |
| | 道德与伦理立场 | 🔵 |
| | 动机与长期目标 | 🔵 |
| | 兴趣与审美偏好 | 🔵 |
| | 自我认知（优缺点、身份认同） | 🟠 |
| **IV · 能力与技能** | 专业能力（计算机、AI 使用、领域专长） | 🔵 |
| | 通用技能（沟通、写作、组织） | 🔵 |
| | 学习能力与学习模式 | 🔵 |
| | 身体技能（运动、手工） | 🔵 |
| **V · 行为与习惯** | 日常作息与生活节律 | 🔵 |
| | 工作与学习习惯 | 🔵 |
| | 决策与行动风格 | 🟠 |
| | 协作与交互偏好 | 🔵 |
| **VI · 社会与处境** | 社会关系（家庭、朋友、同伴、社群） | 🔵 |
| | 社会身份与角色（职业、学历、婚姻、地域） | 🔵 |
| | 文化与人际处境 | 🔵 |
| | 资源条件（经济、时间、工具、环境） | 🔵 |
| **VII · 经历与轨迹** | 成长与生活经历 | 🔵 |
| | 教育与科研 / 工作经历 | 🔵 |
| | 项目与作品史 | 🔵 |
| | 成就、挫折与转折 | 🔵 |

🔵 = 问卷 ＋ 观察 ｜ 🟠 = 仅观察（S）

---

## 三、S 集合（仅观察）

| 边 | 为什么 |
|---|---|
| 思维方式与思辨 | 自评「我逻辑好」是最不可靠的一类自陈 |
| 人格特质 | 人看不清自己的气质与韧性 |
| 情绪模式与心理强度 | 身在情绪里，看不见自己的模式 |
| 自我认知（优缺点、盲区） | 自陈不可靠的典型 |
| 决策与行动风格 | 人对「自己怎么决策」的叙述常与行为相反 |

---

## 四、表达约束生成（S2 核心）

### 4.1 标尺：知识专业度 1–10

| 档位 | 含义 | 表达要求 |
|---|---|---|
| **1–3** | 科普 / 大众 | 不用术语，非用不可就当场解释；大量类比；短、浅、先给结论 |
| **4–6** | 本科（**默认锚点**） | 常用术语直接用，生僻的随手解释；适度类比；中等，可带推理 |
| **7–8** | 硕士 / 资深 | 术语自由；少用类比；可深入 |
| **9–10** | 博士 / 专家 | 术语自由，含行话缩写；几乎不用类比；完整，含边界与不确定 |

**快捷式**：`/or 把我当作（）岁小孩`（临时降级用；「岁」本身也是分领域的）

> **落库口径（S1/S2 已定）**：四档的**枚举值**用 `KNOWLEDGE_TIERS = ['科普','本科','硕士','专家']`（`lib/constants.mjs`），上表「硕士 / 资深」「博士 / 专家」是含义栏的写法，不必造两个词。`profile.tier` 存枚举值，缺省由 `tierForLevel(level)` 推导；`lib/constraint.mjs` 的 `TIER_RULES` 与它逐值比对，加载期不一致即抛。

### 4.2 分领域取值

同一把尺，每个板块各量一个值。例：计算机 8 / 数学 5 / 生物 3。

### 4.3 注入模板

```
【表达约束】
用户水平（按当前话题领域取用）：
· 计算机/AI 8/10（资深）
· 数学 5/10（本科）
· 生物 3/10（科普）

当前话题落在「计算机」→ 按 8 级表达：
术语可直接用，不做解释；少用类比；可深入。

若话题切换到其他领域，改按对应档位表达。
```

### 4.4 形式约束（从 V 交互偏好取）

- **术语处理**（见 4.1 表）
- **类比偏好**：吃类比，还是嫌啰嗦
- **篇幅与结构**：一次一段，还是先结论再展开

### 4.5 处理链

```
多边形取分领域水平 → 过四档映射表 → 加交互偏好 → 拼成注入文本
```

---

## 五、施工顺序

| 步 | 做什么 | 借力 | 自己写 |
|---|---|---|---|
| **S0 · 立基座** | 起 repo、拉骨架、装开发知识；选基座 | `create-dsh-plugin` ＋ `dsh-plugin-guide`；`dsh-experience-memory`（结构最像）/ `dsh-memento`（架构最净） | 选型判断 |
| **S1 · 存储与检索** | 建多边形 schema（七面 × 子板块 ＋ status ＋ 来源 ＋ 置信度），接中文检索 | 基座 SQLite 层 ＋ `dsh-knowledge-sqlite`（FTS5 trigram） | **多边形 schema** |
| **S2 · 注入闭环** | 分路注入 ＋ 表达约束 ＋ 稳定前置 | `dsh-experience-memory` 分路 ＋ `dsh-lib-context-injection` ＋ 讨论 #4749 结论 | **表达约束生成** |
| **S3 · 问卷** | 做成 DSH 原生 skill | DSH 原生 skill（`$DSH_HOME/skills`）＋ `skill-injector-plugin` 参考 | 题目设计 |
| **S4 · 观察** | 后台扫历史对话、推断 S 板块 | 基座子代理 review ＋ `ctx.jobs` | 推断逻辑与提示词 |
| **S5 · 治理** | soft delete、观察优先仲裁、置信门槛 | `dsh-memento` 审批门 | **soft delete ＋ 仲裁** |
| **S6 · Dream** | 后台整合 | — | 备忘录，不做 |

**必须自己写、无轮子可借的四样**：多边形 schema、表达约束生成、soft delete 与观察优先仲裁、观察的推断逻辑。

---

## 六、可复用的轮子

| 轮子 | 是什么 | 省下 |
|---|---|---|
| `create-dsh-plugin`（官方） | 一条命令生成插件骨架，自带 `--verify` 冒烟 | Cordis 摸索期 |
| `dsh-plugin-guide` | 插件开发知识库，按需 skill | 边写边查文档 |
| `dsh-experience-memory`（MIT） | 常驻注入 ＋ 按需检索 ＋ 审批开关 ＋ 后台子代理 review ＋ SQLite/FTS5 ＋ 反馈加权排序 | 几乎整个架构 |
| `dsh-memento`（Apache-2.0） | `ctx.memory` 能力接缝 ＋ SQLite provider ＋ 冻结快照注入 ＋ 审批门 | 「独立模块 ＋ 单接口」范式 |
| `dsh-knowledge-sqlite` | FTS5 trigram 检索，专修中文子串 | 中文检索坑 |
| `dsh-lib-context-injection` | 上下文注入库 | 注入层 |
| `dsh-context-doctor` | 注入 token 成本审计、重复冲突检测 | 调预算的仪表 |
| 讨论 #4749 | 稳定内容前置，缓存命中 42%→94% | 缓存纪律参考答案 |

---

## 七、备忘录

- **Dream**：后台整合记忆。暂不做，留作后续锦上添花。

---

## 八、实施记录

| 日期 | 事项 | 结果 |
|---|---|---|
| 2026-09-13 | **S0 · 立基座** | repo 起于 `D:\GitHub_place\记忆系统`；环境确认（Node v24.15.0 / pnpm 11.22.0 / DSH 在跑）；基座选定 **`dsh-memento`**（capability seam 式，正合「独立模块 ＋ 单接口」），源码已入位；功能参考候选 `dsh-experience-memory` 留在 `_candidates/` 备查；开发知识 `dsh-plugin-guide` 拉入 `_refs/` |
| 2026-09-13 | S0 · 改造蓝图 | ✅ 已落盘 `docs/改造蓝图.md`（4 节 / 75 行）。结论：骨架（审批门 ＋ SQLite ＋ 审计 ＋ retrieval seam）可复用；七面模型、分路注入、表达约束、soft delete 四件事须新造。最重的一锤是**观察通道**——memento 的红线是「适配器只转换、不调模型」，没有「后台分析历史对话」的现成路径 |
| 2026-09-13 | **S1 · 数据模型与检索基座** ✅ | 代码完成：SCHEMA v4→v5；entries 加 `facet` / `level` / `status` 三列 ＋ 新建 profile 表；七面 / 知识领域（8 大类 31 子领域）/ 四档常量与 `tierForLevel` 就位；types 与校验同步。完整测试：**181 通过 / 6 失败**，S1 相关全绿零回归；6 个失败均属既存环境边界（5 个沙箱 EPERM）与依赖声明缺失（1 个），与 S1 无关 |
| 2026-09-13 | **S2 · 分路注入与表达约束** ✅ | 新建 `lib/constraint.mjs`（四档锚点表 ＋ `renderConstraint` ＋ `assertTierRules` 加载期一致性自检 ＋ 31 领域钳制）；`lib/snapshot.mjs` 拆出 `renderWarmup`（预热段 = 表达约束 ＋ 常驻画像 ＋ 提案块）与 `residentEntries`，原 `renderSnapshot` 保留；`index.mjs` 注入位改走预热段（**冻结机制不变**，WeakMap 按 Session 冻结）；`strings.mjs` 加预热头 / 约束头 / 画像标题 / 四档说话要求词表。测试 **207 / 207 全绿**（新增 18 条：constraint 11 ＋ snapshot 7）；门链 lint / typecheck（0 新增错误）/ check:coverage（lib ≥90%、all ≥90%）/ check:readmes / verify:self-contained / verify:artifacts 全过。**A 项结论**：`@deepseek-ai/dsh-system-prompt` 与 `dsh-user-approval` 声明早已在 `devDependencies`，实测六个 peer 包 import 全通；先前的解析失败源于顶层存在空壳目录，已随 junction 修复消失，无需再改声明 |

**S2 分路口径（防止后续误读）**：预热段只取 **user 轨 × scope=user-global**；工作区层（`scope=workspace`）与 agent 轨条目**刻意不进预热段**，仍走既有 `memory_recall` 工具 ＋ retrieval seam 按需取。这条取舍的代价见下方「已知未闭合项」。

| 2026-09-13 | **S2 · 补针（类型门收口）** ✅ | 修完 8 条类型报错（收敛 5 处根因，全部 JSDoc 注解与内联转型，**零运行期语句变更**）；`npm run typecheck` **0 错误**、`npm test` **207 / 207 全绿**、lint 0 errors。改动：`index.mjs:74`、`lib/store.mjs`（112 / 193-196 / 208 / 628-631 / 645 / 589）、`lib/protocol.mjs:111` |

**基座选择理由**：memento 是 capability seam（`ctx.memory` 服务 ＋ provider），本身就是「独立模块 ＋ 单一接口」的现成范式；模块边界干净（gate / snapshot / store / retrieval / budget），改造时每处机制有独立落点；且它守 DSH 硬约束。代价：要把「两轨两层」扩成「七面」、把「冻结快照」改成分路注入——这两项落在 S1 / S2。

### 已知未闭合项（S2 留账，别当已完成）

| # | 事项 | 现状 | 影响 |
|---|---|---|---|
| 1 | **工作区/agent 轨条目失去自动可见性** | 预热段按规格只放 user-global；`memory_recall` 需要模型先「想到去查」 | 项目约定、环境教训可能整场不被想起。缓解：预热头已写明「按需取」，工具描述已写明分路。真正的解法（如常驻一行目录 / 会话首轮提示 / 情境触发）留给 S4 观察通道 |
| 2 | **表达约束段的 token 成本** | 每次会话固定注入四档规则表（中文约 350 字，含 1 个领域时约 300 字） | 规格 4.1 要求每档一句「说话要求」，照做即此成本。若嫌贵，可只在场当前档 + 默认档一句 |
| 3 | **四档 → `profile.tier` 用词** | 枚举取 `KNOWLEDGE_TIERS` 的「硕士」；清单含义栏写作「硕士 / 资深」 | 已在上文 4.1 加落库口径说明，测试断言两表逐值一致 |
| 4 | ~~typecheck 6 条既存错误~~ **已闭合（0913-17）** | 修完 8 条报错（5 处根因，全在注解面）：`index.mjs:74`、`lib/store.mjs:112/193-196/208/628-631/645/589`、`lib/protocol.mjs:111` | `npm run typecheck` **0 错误**；`npm test` **207/207 全绿**；零运行期语句变更 |

---

*本文件为弹性草稿，中途变直接改本文件。*
