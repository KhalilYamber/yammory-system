# DSH 用户画像记忆插件 · 施工清单

> **目标**：让 DSH 知道用户的分领域知识水平与完整画像，在对话中自动用对方听得懂的话交流，提高对话效率与学习效率。
> **形态**：给 DSH 开发一个独立记忆模块（Cordis 插件）。
> **状态**：架构定稿，待施工。

---

## 一、定稿架构

| 面 | 定了什么 |
|---|---|
| **形态** | 独立模块，与 agent 双向互通（读画像 / 写更新）；对外只一个权限门 ＋ 一组读写工具 |
| **数据模型** | 多边形；固定 WBS 清单（七面 × 子板块），穷尽覆盖、允许空白。**落库方式：混着来** = 每面用 `facet` 标签挂条目（轻）＋ 分领域知识水平单开 profile 表（结构化） |
| **颗粒度** | 周级、能力层概括（非日流水、非知识点细节） |
| **存储** | SQLite 为主（FTS5 检索，用 trigram 修中文）；Markdown 可选镜像；后端留可插拔口 |
| **采集** | 双通道。问卷（覆盖 U−S，**用户主动激发 skill**）＋ 隐性观察（覆盖全部，**后台离线分析历史对话**） |
| **S 判据** | 自陈不可靠 ∪ 主观性强 |
| **更新** | 以当次为准覆盖旧记忆；**soft delete 留痕**（active → superseded），可回滚 |
| **仲裁** | **分面裁决**：能力听观察 / 意愿听自陈 / 落差两条都留（口径见文末「S3 口径」第 6 条） |
| **注入** | 单套：按需注入为主 ＋ 开场限流预热；按内容分路（普遍相关预热 / 情境相关按需） |
| **表达约束** | 分领域水平 → 四档映射 → 拼注入文本（见第四节）。**会话内冻结**：新会话开始算一次，会话中不变 |
| **知识领域刻度** | 固定清单，8 大类 / 31 子领域，见 `docs/知识领域清单.md` |
| **权重** | agent 按任务自动分配；默认全自动，设置里可切半自动；无全手动 |
| **注入量** | 由内容繁荣度自动决定（膨胀问题后续处理） |
| **纪律** | 稳定内容前置、限流、写入留门；策略集中写一处，不做开关、不做界面、不做实验 |
| **维护** | Dream（备忘录，暂不做） |

---

## 二、多边形（七面 × 子板块）

| 顶层 | 子板块 | 通道 |
|---|---|---|
| **I · 躯体** | 生理与健康（体质、体能、疾病） | 🔵 |
| | 睡眠与饮食节律 | 🔵 |
| | 感官与身体条件 | 🔵 |
| **II · 心智** | 认知能力（智力、记忆、注意） | 🔵 |
| | 知识与学科水平 | 🔵 |
| | 思维方式与思辨 | 🟠 |
| | 人格特质 | 🟠 |
| | 情绪模式与心理强度 | 🟠 |
| **III · 价值与意愿** | 三观与信仰 | 🔵 |
| | 道德与伦理立场 | 🔵 |
| | 动机与长期目标 | 🔵 |
| | 兴趣与审美偏好 | 🔵 |
| | 自我认知（优缺点、身份认同） | 🟠 |
| **IV · 能力与技能** | 专业能力（计算机、AI 使用、领域专长） | 🔵 |
| | 通用技能（沟通、写作、组织） | 🔵 |
| | 学习能力与学习模式 | 🔵 |
| | 身体技能（运动、手工） | 🔵 |
| **V · 行为与习惯** | 日常作息与生活节律 | 🔵 |
| | 工作与学习习惯 | 🔵 |
| | 决策与行动风格 | 🟠 |
| | 协作与交互偏好 | 🔵 |
| **VI · 社会与处境** | 社会关系（家庭、朋友、同伴、社群） | 🔵 |
| | 社会身份与角色（职业、学历、婚姻、地域） | 🔵 |
| | 文化与人际处境 | 🔵 |
| | 资源条件（经济、时间、工具、环境） | 🔵 |
| **VII · 经历与轨迹** | 成长与生活经历 | 🔵 |
| | 教育与科研 / 工作经历 | 🔵 |
| | 项目与作品史 | 🔵 |
| | 成就、挫折与转折 | 🔵 |

🔵 = 问卷 ＋ 观察 ｜ 🟠 = 仅观察（S）

---

## 三、S 集合（仅观察）

| 边 | 为什么 |
|---|---|
| 思维方式与思辨 | 自评「我逻辑好」是最不可靠的一类自陈 |
| 人格特质 | 人看不清自己的气质与韧性 |
| 情绪模式与心理强度 | 身在情绪里，看不见自己的模式 |
| 自我认知（优缺点、盲区） | 自陈不可靠的典型 |
| 决策与行动风格 | 人对「自己怎么决策」的叙述常与行为相反 |

---

## 四、表达约束生成（S2 核心）

### 4.1 标尺：知识专业度 1–10

| 档位 | 含义 | 表达要求 |
|---|---|---|
| **1–3** | 科普 / 大众 | 不用术语，非用不可就当场解释；大量类比；短、浅、先给结论 |
| **4–6** | 本科（**默认锚点**） | 常用术语直接用，生僻的随手解释；适度类比；中等，可带推理 |
| **7–8** | 硕士 / 资深 | 术语自由；少用类比；可深入 |
| **9–10** | 博士 / 专家 | 术语自由，含行话缩写；几乎不用类比；完整，含边界与不确定 |

**快捷式**：`/or 把我当作（）岁小孩`（临时降级用；「岁」本身也是分领域的）

> **落库口径（S1/S2 已定）**：四档的**枚举值**用 `KNOWLEDGE_TIERS = ['科普','本科','硕士','专家']`（`lib/constants.mjs`），上表「硕士 / 资深」「博士 / 专家」是含义栏的写法，不必造两个词。`profile.tier` 存枚举值，缺省由 `tierForLevel(level)` 推导；`lib/constraint.mjs` 的 `TIER_RULES` 与它逐值比对，加载期不一致即抛。

### 4.2 分领域取值

同一把尺，每个板块各量一个值。例：计算机 8 / 数学 5 / 生物 3。

### 4.3 注入模板

```
【表达约束】
用户水平（按当前话题领域取用）：
· 计算机/AI 8/10（资深）
· 数学 5/10（本科）
· 生物 3/10（科普）

当前话题落在「计算机」→ 按 8 级表达：
术语可直接用，不做解释；少用类比；可深入。

若话题切换到其他领域，改按对应档位表达。
```

### 4.4 形式约束（从 V 交互偏好取）

- **术语处理**（见 4.1 表）
- **类比偏好**：吃类比，还是嫌啰嗦
- **篇幅与结构**：一次一段，还是先结论再展开

### 4.5 处理链

```
多边形取分领域水平 → 过四档映射表 → 加交互偏好 → 拼成注入文本
```

---

## 五、施工顺序

| 步 | 做什么 | 借力 | 自己写 |
|---|---|---|---|
| **S0 · 立基座** | 起 repo、拉骨架、装开发知识；选基座 | `create-dsh-plugin` ＋ `dsh-plugin-guide`；`dsh-experience-memory`（结构最像）/ `dsh-memento`（架构最净） | 选型判断 |
| **S1 · 存储与检索** | 建多边形 schema（七面 × 子板块 ＋ status ＋ 来源 ＋ 置信度），接中文检索 | 基座 SQLite 层 ＋ `dsh-knowledge-sqlite`（FTS5 trigram） | **多边形 schema** |
| **S2 · 注入闭环** | 分路注入 ＋ 表达约束 ＋ 稳定前置 | `dsh-experience-memory` 分路 ＋ `dsh-lib-context-injection` ＋ 讨论 #4749 结论 | **表达约束生成** |
| **S3 · 问卷** | 做成 DSH 原生 skill | DSH 原生 skill（`$DSH_HOME/skills`）＋ `skill-injector-plugin` 参考 | 题目设计 |
| **S4 · 观察** | 后台扫历史对话、推断 S 板块 | 基座子代理 review ＋ `ctx.jobs` | 推断逻辑与提示词 |
| **S5 · 治理** | soft delete、**分面裁决**（能力听观察 / 意愿听自陈 / 落差并存）、置信门槛 | `dsh-memento` 审批门 | **soft delete ＋ 分面裁决** |
| **S6 · Dream** | 后台整合 | — | 备忘录，不做 |

**必须自己写、无轮子可借的四样**：多边形 schema、表达约束生成、soft delete 与**分面裁决**、观察的推断逻辑。

---

## 六、可复用的轮子

| 轮子 | 是什么 | 省下 |
|---|---|---|
| `create-dsh-plugin`（官方） | 一条命令生成插件骨架，自带 `--verify` 冒烟 | Cordis 摸索期 |
| `dsh-plugin-guide` | 插件开发知识库，按需 skill | 边写边查文档 |
| `dsh-experience-memory`（MIT） | 常驻注入 ＋ 按需检索 ＋ 审批开关 ＋ 后台子代理 review ＋ SQLite/FTS5 ＋ 反馈加权排序 | 几乎整个架构 |
| `dsh-memento`（Apache-2.0） | `ctx.memory` 能力接缝 ＋ SQLite provider ＋ 冻结快照注入 ＋ 审批门 | 「独立模块 ＋ 单接口」范式 |
| `dsh-knowledge-sqlite` | FTS5 trigram 检索，专修中文子串 | 中文检索坑 |
| `dsh-lib-context-injection` | 上下文注入库 | 注入层 |
| `dsh-context-doctor` | 注入 token 成本审计、重复冲突检测 | 调预算的仪表 |
| 讨论 #4749 | 稳定内容前置，缓存命中 42%→94% | 缓存纪律参考答案 |

---

## 七、备忘录

- **Dream**：后台整合记忆。暂不做，留作后续锦上添花。

---

## 八、实施记录

| 日期 | 事项 | 结果 |
|---|---|---|
| 2026-09-13 | **S0 · 立基座** | repo 起于 `D:\GitHub_place\记忆系统`；环境确认（Node v24.15.0 / pnpm 11.22.0 / DSH 在跑）；基座选定 **`dsh-memento`**（capability seam 式，正合「独立模块 ＋ 单接口」），源码已入位；功能参考候选 `dsh-experience-memory` 留在 `_candidates/` 备查；开发知识 `dsh-plugin-guide` 拉入 `_refs/` |
| 2026-09-13 | S0 · 改造蓝图 | ✅ 已落盘 `docs/改造蓝图.md`（4 节 / 75 行）。结论：骨架（审批门 ＋ SQLite ＋ 审计 ＋ retrieval seam）可复用；七面模型、分路注入、表达约束、soft delete 四件事须新造。最重的一锤是**观察通道**——memento 的红线是「适配器只转换、不调模型」，没有「后台分析历史对话」的现成路径 |
| 2026-09-13 | **S1 · 数据模型与检索基座** ✅ | 代码完成：SCHEMA v4→v5；entries 加 `facet` / `level` / `status` 三列 ＋ 新建 profile 表；七面 / 知识领域（8 大类 31 子领域）/ 四档常量与 `tierForLevel` 就位；types 与校验同步。完整测试：**181 通过 / 6 失败**，S1 相关全绿零回归；6 个失败均属既存环境边界（5 个沙箱 EPERM）与依赖声明缺失（1 个），与 S1 无关 |
| 2026-09-13 | **S2 · 分路注入与表达约束** ✅ | 新建 `lib/constraint.mjs`（四档锚点表 ＋ `renderConstraint` ＋ `assertTierRules` 加载期一致性自检 ＋ 31 领域钳制）；`lib/snapshot.mjs` 拆出 `renderWarmup`（预热段 = 表达约束 ＋ 常驻画像 ＋ 提案块）与 `residentEntries`，原 `renderSnapshot` 保留；`index.mjs` 注入位改走预热段（**冻结机制不变**，WeakMap 按 Session 冻结）；`strings.mjs` 加预热头 / 约束头 / 画像标题 / 四档说话要求词表。测试 **207 / 207 全绿**（新增 18 条：constraint 11 ＋ snapshot 7）；门链 lint / typecheck（0 新增错误）/ check:coverage（lib ≥90%、all ≥90%）/ check:readmes / verify:self-contained / verify:artifacts 全过。**A 项结论**：`@deepseek-ai/dsh-system-prompt` 与 `dsh-user-approval` 声明早已在 `devDependencies`，实测六个 peer 包 import 全通；先前的解析失败源于顶层存在空壳目录，已随 junction 修复消失，无需再改声明 |

**S2 分路口径（防止后续误读）**：预热段只取 **user 轨 × scope=user-global**；工作区层（`scope=workspace`）与 agent 轨条目**刻意不进预热段**，仍走既有 `memory_recall` 工具 ＋ retrieval seam 按需取。这条取舍的代价见下方「已知未闭合项」。

| 2026-09-13 | **S2 · 补针（类型门收口）** ✅ | 修完 8 条类型报错（收敛 5 处根因，全部 JSDoc 注解与内联转型，**零运行期语句变更**）；`npm run typecheck` **0 错误**、`npm test` **207 / 207 全绿**、lint 0 errors。改动：`index.mjs:74`、`lib/store.mjs`（112 / 193-196 / 208 / 628-631 / 645 / 589）、`lib/protocol.mjs:111` |

**基座选择理由**：memento 是 capability seam（`ctx.memory` 服务 ＋ provider），本身就是「独立模块 ＋ 单一接口」的现成范式；模块边界干净（gate / snapshot / store / retrieval / budget），改造时每处机制有独立落点；且它守 DSH 硬约束。代价：要把「两轨两层」扩成「七面」、把「冻结快照」改成分路注入——这两项落在 S1 / S2。

| 2026-09-13 | **S3 · 问卷通道（插件侧接线 ＋ 原生 skill）** ✅ | ①`memory` 工具补 `facet`（七面 enum）与 `level`（1..10）入参，`add`/`replace`/`consolidate` 规范化后透传；`replace` 省略坐标即保持原值，`query` 回带坐标。②新增 `memory_profile` 工具（`set`/`list`/`get`），补上 profile 表自 v5 建表以来一直缺的对外写入通道：`domain` 过 `KNOWLEDGE_DOMAINS`（31 项）校验、`level` 限 1..10、`tier` 缺省由 `tierForLevel` 推导、写走 `MemoryProtocolCore.setProfile` 的审批门 ＋ `profile-set` 审计（工具层绕不过）。③`skills/yammory-survey/`：DSH 原生 skill（`SKILL.md` ＋ `references/question-bank.md`），含触发纪律、写入协议、颗粒度红线、分批策略与题库。测试 **214 / 214 全绿**（新增 7 条：工具接线 5 ＋ Provider 2）；门链 lint / typecheck（0 错）/ check:coverage（lib ≥90%、all ≥90%）/ check:readmes（五语同步）/ verify:self-contained / verify:artifacts / **verify:skill（新增门）** 全过 |
| 2026-09-13 | **S3 · 问卷 skill 改版（针与布）** ✅ | 按用户定稿重写 `skills/yammory-survey/`：① 定位改为「问卷是补充、观察是大头」，加**预算纪律**（问卷条目长期占比 ≤ 约⅓）；② 一轮改为**至多 7 题**，第七题后停下由用户选「继续 / 歇 / 停」，**不设总量上限**；③ 挑板块改「**两端优先**」（用户指定 > 太热 > 太冷 > 其余）；④ 触发放宽（提及记忆/画像/问卷即可）；⑤ 题型由 agent 按题定，题目要求一行说完；⑥ 含糊判据落成可操作版：**每题标 3–4 个要点，答清 ≥70% 即过、<70% 只追问一次**；⑦ 知识水平改「**抬头半档问**」（以现有档为锚上抬，接得住上调、接不住守档）；⑧ 条目允许 2–3 句；⑨ 新增「**重问按性子不按日历**」（半衰期 ＋ 事件触发）；⑩ 新增「**与观察的边界：分面裁决**」（能力听观察 / 意愿听自陈 / 落差两条都留） |

| 2026-09-13 | **S4 · 观察通道（探路与选型）** ✅ 只读轮 | 交付 `docs/观察通道方案.md`（结论先行 ＋ 选型 ＋ 六问逐条 ＋ 证据索引）。**未改源码、未提交、未动数据面**。**选型结论**：落点 = 模型面工具（`memory_observe`，`scan` 取切片 / `commit` 批量落库）＋ 原生 skill（`yammory-observe` 承载推断提示词）；**推断交给当前会话的模型**，v1 显式触发、默认不自动跑。**逐项裁决**：① `ctx.sessionQuery` ✅ 采用（公开服务，本机 active，项目已有 `recallHistory` 先例）；② `ctx.llm` 直调 ❌ 排除——撞项目红线「模型可见 ⟺ 落盘」（请求不进任何会话日志，审计链在推断这步断掉）；③ `ctx.jobs` ❌ 排除——本机 `tool-jobs` 是 `[disabled]`，`servesOwner` 直接抛，且 jobs 只给生命周期不给模型；④ 子代理 ⏸ 留 v2（成本不可见 ＋ 新会话污染血缘 ＋ 本机模型面 `tool-subagent*` 全 disabled）；⑤ 事件钩子只作未来触发时机；⑥ 直读会话日志文件 ❌ 排除（`session.v3.jsonl.zstd`：zstd ＋ v3 ＋ 迁移梯子 ＋ replay 校验，`readSession` 已做完）。**写入口径**：`source='observation'`（自由字符串，**无需动 schema**）；审批粒度键 `source:observation`（`lib/gate.mjs:96` 正则接受，优先级 `source:<source>` > `track/scope` > 全局）；一次 commit 一次审批（复用 `service.seed` ＋ `consolidate` 的「单审批原子写」模式）；v1 **只做「并存 ＋ 标注」，不做 supersede**（回滚语义留 S5）。**两条实测事实（防后续误读）**：ⓐ base 的 `session-query-sqlite` 配置为 `path: ':memory:'` ＋ `openAt: never`——**精确读取/过滤/title/血缘可用，全文搜索关闭**（`SESSION_QUERY_SEARCH_DISABLED`）；`tool-session-query` 的五个 `session_*` 模型面工具 base **未装配**。ⓑ `entries.facet` 是**七面**枚举（`lib/constants.mjs:13`），而五个观察面（思维方式与思辨 / 人格特质 / 情绪模式与心理强度 / 自我认知 / 决策与行动风格）是**子板块**级——**推荐 v1 用 `facet='心智'` ＋ `tags=['思维方式与思辨']` 承载，零 schema 改动**（S4b 开工前拍板）。**S4b 下一步**：S4b-0 先实测本机 `user/message` 的 `source.kind` 实际枚举（不过滤 `kind:'plugin'` 会把 goal 驱动等伪发言当用户证据，推断全歪）＋ 量一次 12k 字符切片的 token；随后 `lib/observe.mjs`（纯函数：窗口采样/过滤/预算）→ 工具 → skill → 命令 → 门链收口 |
| 2026-09-13 | **F1 · 冷启动 dogfood（真机首跑）** ✅ | 插件以 `link:` 方式挂进 DSH `web` profile（`profiles/web/package.json` 加依赖与 bundle ＋ node_modules 联接），问卷 skill 装到用户级 `~/.dsh/skills`；配置 `writePolicy: auto`（问卷期间不弹审批）＋ `language: zh`。**端到端验证**：`audit` 的 `snapshot` 行含完整预热块，逐字可见【表达约束】（分领域水平 ＋ 四档规则表）＋【常驻画像】（带用量头）；首轮问卷落库 1 个领域刻度 ＋ 数条 user-global 条目；用户体感「agent 的话能听懂大部分」。**首见「自陈 vs 观察」落差**：自评偏保守，而行为证据（已有开源项目交付）更高——正是「分面裁决」要处理的样本，待 S4 观察通道上线后自动浮现 |
| 2026-09-13 | **D1 · 记忆机制 v2 规格** ✅ | 落纸 `docs/记忆机制v2规格.md`（拆上限＋软预警、分级注入、会话开关、静默整理机含收益判据与打标、桶内边界；两处待决已定、一处待 S4）。同轮把 S5 仲裁口径由「观察优先」改为「**分面裁决**」 |
| 2026-09-13 | **F2 · 层 A 检索升级（零依赖）** ✅ | 按 `docs/F2检索升级方案.md` 落 A1–A5。①`lib/retrieval.mjs` 新增 `tokenize`（CJK 相邻二字 bigram ＋ 拉丁/数字整词，去重）与 `KeywordRetriever`（`id='keyword'`、`kind='substring'`；任一词元命中即召回，相关度 = 0.5×覆盖率 ＋ 0.3×词元长度权重 ＋ 0.2 整串精确加成、`min(1,·)` 封顶；排序 = 相关度 DESC → 复用 `rankOrder`；`retrieve` 不截断）。②`index.mjs` 默认装配换 keyword：`live.retriever` 初值 `new KeywordRetriever()`（单例 `keywordRetriever`），vector 开启且探测到 embedding 时换 `VectorRetriever`，否则/降级回落 keyword；两处装配（apply 初始 ＋ settings `onChange`）同步改；`SubstringRetriever` 保留注册（对照 / MCP / 第三方）。③JSDoc、seam 注释、`memory_recall` 描述与 `lib/mcp.mjs` 头注同步。④测试：新增 5 条（分词 2 ＋ 多词召回 1 ＋ 打分排序 1 ＋ 边界 1），改写「默认主路径」为「命中任一词元即可召回」并保留「全无关联零命中」；`rankOrder`/`SubstringRetriever`/`VectorRetriever`/Registry 断言未动。**门链**：`npm test` **220/220 全绿**；lint 0 errors（9 条既存 warning）；typecheck 0 错误；coverage `lib/retrieval.mjs` 99.00% / `index.mjs` 95.26% / all 95.91%（全过门）；`check:readmes` / `verify:self-contained` / `verify:artifacts` 全过。**对照结论**：多词查询由零命中转为可召回（`用户偏好 工作区`、`workspace permission` 等），单词查询行为与旧路径逐字一致，召回面是旧路径的超集。**两条已知边界**（未擅自改方案）：ⓐ词元按「连续字母/数字段」切，CJK 与拉丁同段书写（如「用户偏好abc」）时整段走 bigram、拉丁部分不再成整词；ⓑ滑窗产生跨词 bigram（「用户偏好英文」切出「好英」），故多词查询的排序由覆盖率 ＋ 整串加成共同主导。**两处待决**（未擅自动手）：keyword 未注册进 `ctx.memoryRetrieval`（方案未要求，故 `retrievers.get('keyword')` 为空）；`memory_recall` 工具描述文案已随语义更新（原「大小写不敏感子串」已失真）。**对照脚本**：`dev/f2-recall-compare.mjs`（本地工程面，永不提交） |

| 2026-09-13 | **S4b-0 · 观察通道实测校准** ✅ | 只读实测（探针 `dev/observe-probe.mjs`，本地工程面不提交）。**① `user/message` 的 `source.kind` 实为 9 种，不是方案设想的 3 种**（273 份日志 / 22 个工作区 / 1892 条 user/message）：`user` 982 · `plugin` 351 · `agent-instructions` 279 · `skill-catalog` 261 · `goal` 8 · `subagent-settled` 4 · `subagent-report` 3 · `skill-invocation` 3 · `agent-message` 1。**`user-rpc` 本机 0 出现**——浏览器提交就是 `kind:'user'` ＋ `rpcId`（982 条里 966 条带 rpcId），方案 2.3 把它列为独立 kind 是未兑现假设。**闸二必须是白名单**：未过滤时 1892 条里 **910 条（48.1%）是系统注入的伪发言**（AGENTS.md 注入均长 7672 字符、skill 目录均长 2106 字符，比真人发言中位 33 字符厚两个数量级）。白名单 `{user, user-rpc}` 保住了全部 982 条真人发言。**残余风险（诚实登记）**：16 条无 rpcId 的 `user` 里混有外部桥接注入（如「调用 session_summarize 工具…」），仅凭 kind 无法与真人发言区分——这是白名单的边界，不是可实现可修项。**② 12k 字符的真实 token 成本**：用本机 3730 个「新进上下文字符数 ↔ 真实 inputTokens」配对样本反推（`dev/observe-probe.mjs ratio`）——**中文用户发言 1.86 字符/token（均值）／2.03（中位）**，代码类工具输出 3.21／3.00，全体 3.27／2.82。故 **12000 字符 ≈ 5900–6500 token**（中文发言口径）；**DSH 自带启发式（4 字符/token）低估约一倍**。**③ 本项目工作区实跑**（13 会话 / 14 天内 18 条真人发言）：按方案 2.2 默认口径（14 天 / 8 会话 / 每会话 12 条 / 单条 400 字符 / 总 12000）出 **10 条发言、4000 字符 ≈ 2150 token**——**预算是天花板而非瓶颈，真正的约束是「会话数 × 每会话条数」与单条 400 字符截断**（本工作区真人发言均长 1500 字符，截断吃掉大半信息） |
| 2026-09-13 | **S4b · 观察通道（实现，S4b-1..6）** ✅ | 按 `docs/观察通道方案.md` 第八节落地。**①`lib/observe.mjs`（新，零 DSH 依赖纯函数）**：闸一 `sessionScope`（有 cwd 只读 cwd 精确相等；无 cwd 只读当前会话自己；两者皆无响亮拒绝）、闸二 `isHumanMessageEvent`（白名单 `{user, user-rpc}`）、`extractHumanMessages`（单条截断）+ `sampleEvenly`（首末必留、中间等距）+ `buildObservationSlice`（逐行预算记账，到顶即停并把未覆盖会话数/条数/天数写进 `uncovered`）+ `normalizeObservationEntries`（facet 取七面值、子板块进 tags、`source` 锚死）+ `resolveObserveOptions`（模型放大不了预算）。**②常量与错误**：`lib/constants.mjs` 加 `OBSERVE_LIMITS`/`OBSERVE_KEYS`/`OBSERVATION_FACES`/`OBSERVATION_CORRECTION`/`OBSERVATION_FACE_VALUES`/`OBSERVATION_SOURCE`/`OBSERVATION_TAG`/`MAX_OBSERVATION_ENTRIES` 与错误码 `SESSION_QUERY_UNAVAILABLE`；`lib/errors.mjs` 加 `SessionQueryUnavailableError`；**`lib/protocol.mjs` 的 `seed` 把同源批次的 `source` 带进审批载荷**（混源批次不带）——否则 `source:observation` 粒度键没有着力点，用户设成 `off`/`auto` 会被静默忽略、退回全局策略。**③`index.mjs`**：`Config.observe` 五参三处贯通（Config/SettingsSchema/resolveComposed）＋ 加载期校验硬上限；`memory_observe` 工具（`scan` 走 `ctx.get('sessionQuery')` 只读切片、缺失时 `ok:false` ＋ `SESSION_QUERY_UNAVAILABLE` 响亮降级；`commit` 走 `service.seed`，一次审批 ＋ 一次原子写，审批载荷带 `[source:observation]` 与证据原文）；双语描述与渲染（到顶必报未覆盖）；`/memory observe` 子命令（只读打印同一切片 ＋ 账单，末行指向模型推断，不叫模型不写库）。**④`skills/yammory-observe/`**（`SKILL.md` ＋ `references/observation-facets.md`，落方案第五节提示词与证据门槛），项目级 `.dsh/skills/` 联接后 **DSH catalog 实时发现 `yammory-observe`（实测）**。**⑤五语 README** 同步 `observe.*` 五行配置 ＋ `memory_observe`/`yammory-observe`/`/memory` 三行表面 ＋ 一条「白名单边界」限制说明。**⑥S4b-6 一行目录**（可选收尾项，已做）：预热段末行报「本工作区与 agent 轨另有 N 条记忆不在本块」，只报条数不搬正文；顺带修掉「无约束无画像但有提案/目录行时空出一段」的排版。**测试**：`test/observe.test.mjs`（23 条纯逻辑 ＋ 12 条集成：两条闸端到端、降级、钳制、审批载荷、零部分写入、渲染、命令面）；`test/snapshot.test.mjs` 加一行目录用例。**门链 8/8**：`npm test` **263/263 全绿**；lint 0 errors（9 条既存 warning）；typecheck 0 错误；coverage `lib/observe.mjs` 99.19% / `lib/snapshot.mjs` 97.19% / `lib/strings.mjs` 100% / `index.mjs` 95.62% / all 96.20%（全过门）；`check:readmes` / `verify:self-contained` / `verify:artifacts`（18 个 lib 模块）/ `verify:skill`（2 个 skill）全过。**交付形态**：主体由 0913-27 单在超时后自动续跑写出，0913-29 单复核（发现并发写入、等其静默后接手）＋ 补齐 `skills/README.md`／S4b-6／CHANGELOG／ARCHITECTURE §17／AGENTS 模块表 ＋ 全门链复跑。**两条未做（诚实登记）**：S4b-6 的「值不值得提醒」（哪些工作区条目被反复召回）未做，仍是一行纯条数；`docs/观察通道方案.md` 第七节 7.1-5「切片 12k 是否够」需 3–5 次真实观察实测定标，本轮未跑。 |

| 2026-09-14 | **F5 · 会话级记忆开关** ✅ | 按 `docs/F5会话开关方案.md` 落地。**①`lib/constants.mjs`**：`SCHEMA_VERSION` 5 → **6**；新常量 `SESSION_DEFAULT_ENABLED`（默认开语义锚点）与 `MAX_SWITCH_SESSION_ID`（路由 id 上限 200）；错误码 `SESSION_MEMORY_OFF`。**②`lib/errors.mjs`**：新错误类 `SessionMemoryOffError`（形状照 `WriteDeniedError` 同级，details 带 sessionId）。**③`lib/store.mjs`**：新表 `SESSION_SWITCH_SCHEMA_SQL`（`session_id` 主键 / `enabled` / `updated_at`）＋ 迁移梯子加 `current < 6` 一级；三方法 `sessionEnabled`（无行即开）、`sessionSetEnabled`（关→upsert 0 行；开→删行）、`disabledSessionIds`；输入防御 `isUsableSessionId`（类型谓词，非字符串/空 id 一律视作默认开）。**④`lib/protocol.mjs`**：私有门 `#assertSessionOn(action, write)`——与审批门同级、在 gate 与任何落盘之前；拒绝时落一行 `outcome='session-off'` 审计（`text: null`、`session_id` 照记）再抛。挂载点：`#validateEntry`（覆盖 add/replace/consolidate）＋ `remove`/`seed`/`setProfile` 各自开头。**⑤`index.mjs`**：预热段回调（关→删 WeakMap 冻结缓存＋返回空串，开关优先于会话内冻结，不落 `snapshot`）；`memory` 工具 `query` 与 `memory_recall`（关→结构化 `ok:false` ＋ `SESSION_MEMORY_OFF`，不检索、不 bumpRecall、不落 `recalled`）；`scanObservationHistory`（当前会话关→拒扫；选区先滤 `disabledSessionIds` 再截断，被滤数进 `skippedOff`）；`/memory session [on｜off｜status]`（短码显示、切换落 `session-switch` 审计行、`text: null`）；`GET/POST /api/memento/session`（**一条**路由按 method 分派——注册表以 path 为键，同 path 两条会撞 duplicate route；POST 只收 `{sessionId, enabled}`，多余字段与非法 id → 400）。**⑥`lib/strings.mjs`**：`COMMAND_TEXT`（含 `CommandTextBundle` typedef）从 `index.mjs` 整体搬入，新增 8 条会话开关文案（en/zh）并补齐 `usage` / `unknownVerb` / 帮助行。**⑦`client/client.js`**：`conversation.composer.dock`（session scope）注册开关钮（`SWITCH_STRINGS` en/zh；读 GET、写 POST、就地刷新；拿不到 `sessionId` 或注册被拒时降级只读显示，绝不改宿主）。**⑧`test/session-switch.test.mjs`（新，11 条）**。**⑨文档**：CHANGELOG、ARCHITECTURE 决策 13、AGENTS 模块表与 schema 版本、五语 README（`/memory` 行 ＋ session switch 表面行 ＋ 会话开关要点）、本规格 3.4 现状回填。**门链 8/8**：`npm test` **284/284 全绿**（基线 273 ＋ 新增 11）；lint 0 errors；typecheck 0 错误；coverage `lib/strings.mjs` 100% / `lib/store.mjs` 97.96% / `lib/protocol.mjs` 95.24% / `index.mjs` 95.39% / all 96.25%（全过门；基线 all 96.17%）；`check:readmes` / `verify:self-contained` / `verify:artifacts` / `verify:skill` 全过。**三处方案与现状的差异（已按方案优先处理并登记）**：ⓐ 预热段拦截**不落** `snapshot` 审计——实现按方案做（关了就不留痕），与「模型可见 ⟺ 落盘」不冲突，因为空段本身没有可见内容；ⓑ 方案写「两条路由 GET/POST」，实测 `connection.fetch` 注册表以 path 为键（同 path 第二条抛 `duplicate Fetch route`），故合并为**一条**路由按 `request.method` 分派，栅栏、方法集与 400 行为不变；ⓒ 方案要求「ARCHITECTURE.md 加决策 18」，但该文档实际只写到**决策 12**（正文却引用「已按决策 16 落地」，13–17 从未写下），故落为**决策 13**。**未做（诚实登记）**：真机 Web profile 端到端手验（验收 §5.2/§5.3）本轮未跑——改的是 host 代码，需重启 DSH 才装载；命令面与路由已有集成测试覆盖。
| 2026-09-14 | **F6 · 整理机（v1 增量版）＋ F7 · 可观测三数** ✅ | 按 `docs/F6F7施工方案.md` §3 落地。**①`lib/store.mjs`**：`entries.status` 接通写入语义——新 `supersedeEntries({ids, text?, tags?, ...})`（事务内批量置 `superseded`，可选同事务落合并条目；未知 id / 重复 id / 已降级目标一律 `INVALID_INPUT` 并整批回滚）、`entryById`（含降级条目可查）、`allEntries`（全量，管理面用）；读路径（`listEntries` / `queryEntries` / `matchCandidates` / `usage`）排除 `status='superseded'`。**②`lib/protocol.mjs`**：`MERGED_TAG='merged'`；新动作 `supersede(input, write)`——`#assertAgent` ＋ `#assertSessionOn('supersede')`（与审批门同级、先于 gate）＋ 审批前定位校验（未知 id / 已降级 / 跨桶 / 出会话可见集一律响亮失败）＋ 一次审批 ＋ 一个事务；降级审计行 `text` 恒为 `null`（只记 id），新条目记 `supersede-add`，每批收尾落一行 `consolidation`（兼作开工线的时间锚）；`query()` 的 `recalled` 行按命中数给 `ok` / `empty`。**③`lib/consolidate.mjs`（新，纯函数，100% 行覆盖）**：`selectCandidates`（近 7 天更新 ∪ 召回次数达线；跳 `merged` 与已降级并计数）、`bucketKeyOf`/`groupByBucket`（桶 = track × scope × agentKey，workspace 层追加 workspaceKey）、`similarPairs`（桶内 Jaccard 线索，每桶 ≤12）、`backlogOf`（字符 / 条数 / 12 小时三条线）、`buildTidyPlan`。**④`lib/stats.mjs`（新，纯函数，100% 行覆盖）**：`repetitionStats`（tokenize ＋ Jaccard，O(n²)，超比较上限如实标截断）、`recallHitStats`（ok / empty / 未知三档）、`injectionStats`（`snapshot` 行的字符与条目行数）、`buildStats`（**`successRate` 恒 null**——成功率没有信号源，不拿别的数冒充）。**⑤`index.mjs`**：`memory` 工具加 `tidy`（只读计划）/ `supersede`（写）两个动作与 `ids` 入参（工具描述与参数双语同步）；`/memory tidy [--days=N]` 与 `/memory stats` 两条子命令；`agent/turn-stopping` 只读积压检查（过线落一行 `tidy-due`，同进程按小时节流，整体吞异常——该事件串行派发，抛错会以错误收尾该轮）；预热段末行过线提示；`GET /api/memento/stats`（同 `connection.fetch` 栅栏，第 5 条路由）；`recallViaRetriever` 零命中补记 `empty`。**⑥`skills/yammory-tidy/`（新）**：`SKILL.md` ＋ `references/merge-rules.md`（三档结论、逐对检查表、正反例、批与桶边界）。**⑦文档**：CHANGELOG、ARCHITECTURE 决策 18/19、AGENTS 模块表与红线、五语 README（要点 ＋ `memory` 行 ＋ `/memory` 行 ＋ `yammory-tidy` 行）、本规格 3.5 现状回填、`skills/README.md` 索引。**测试**：`test/consolidate.test.mjs`（8）＋ `test/stats.test.mjs`（6）＋ `test/tidy.test.mjs`（19），共 **318/318 全绿**（基线 285，新增 33）；路由数断言 4 → 5（`test/v2.test.mjs`、`test/lifecycle.test.mjs`、`test/composition.test.mjs` ＋ `scripts/loader-runner.mjs`）。**门链 8/8**：`npm test` 318/318；lint 0 errors（9 条既存 warning）；typecheck 0 错误；coverage `lib/consolidate.mjs` 100% / `lib/stats.mjs` 100% / `lib/store.mjs` 98.15% / `lib/protocol.mjs` 95.43% / `index.mjs` 95.48% / all 96.52%（全过门）；`check:readmes`（五语同步）/ `verify:self-contained` / `verify:artifacts`（20 个 lib 模块）/ `verify:skill`（3 个 skill）全过。**方案与现状冲突（已按方案优先并登记）**：ⓐ §1.2 同时要求「用 `consolidate` 合并」与「旧条目 `status='superseded'`、不物理删」，而既有 `consolidate` 的实现是物理 `DELETE`——两者不可同时成立；本轮按「不物理删」这条更具体的红线实现，`supersede` 承接合并（可选 `text`）＋ 降级，语义是 consolidate 的留痕版。ⓑ §1 说「桶内不跨 = 同 track×scope×agentKey」，但 workspace 层的工作区身份属于「层」本身——桶键追加 `workspaceKey`，否则会把 A 工作区的正文并进 B 工作区。ⓒ §3 未点名 `lib/strings.mjs`，故 tidy/stats 文案内联在 `index.mjs` 的 `TIDY_TEXT`，只在既有 `usage`/`unknownVerb` 文本尾部追加动词。ⓓ §3 的「ARCHITECTURE 决策 14/15」编号已过期（14/15 早被 schema v4 与适配器注册表占用），本轮落为**决策 18/19**。ⓔ §3 未点名 `scripts/loader-runner.mjs` 与 `skills/README.md`，但二者分别钉住路由数与 skill 索引，不同步即留下失真，已一并更新。**未做项**：面板三数只见于 `GET /api/memento/stats` 数据面，**面板 UI 那一行未渲染**（`client/client.js` 不在 §3 点名文件内）；`/memory export` 仍只导出在场条目（导出信封没有 `status` 字段，导回来会变成在场条目）。 |

| 2026-09-14 | **F5 · 会话级记忆开关** ✅ | 按 `docs/F5会话开关方案.md` 落地。**①`lib/constants.mjs`**：`SCHEMA_VERSION` 5 → **6**；新常量 `SESSION_DEFAULT_ENABLED`（默认开语义锚点）与 `MAX_SWITCH_SESSION_ID`（路由 id 上限 200）；错误码 `SESSION_MEMORY_OFF`。**②`lib/errors.mjs`**：新错误类 `SessionMemoryOffError`（形状照 `WriteDeniedError` 同级，details 带 sessionId）。**③`lib/store.mjs`**：新表 `SESSION_SWITCH_SCHEMA_SQL`（`session_id` 主键 / `enabled` / `updated_at`）＋ 迁移梯子加 `current < 6` 一级；三方法 `sessionEnabled`（无行即开）、`sessionSetEnabled`（关→upsert 0 行；开→删行）、`disabledSessionIds`；输入防御 `isUsableSessionId`（类型谓词，非字符串/空 id 一律视作默认开）。**④`lib/protocol.mjs`**：私有门 `#assertSessionOn(action, write)`——与审批门同级、在 gate 与任何落盘之前；拒绝时落一行 `outcome='session-off'` 审计（`text: null`、`session_id` 照记）再抛。挂载点：`#validateEntry`（覆盖 add/replace/consolidate）＋ `remove`/`seed`/`setProfile` 各自开头。**⑤`index.mjs`**：预热段回调（关→删 WeakMap 冻结缓存＋返回空串，开关优先于会话内冻结，不落 `snapshot`）；`memory` 工具 `query` 与 `memory_recall`（关→结构化 `ok:false` ＋ `SESSION_MEMORY_OFF`，不检索、不 bumpRecall、不落 `recalled`）；`scanObservationHistory`（当前会话关→拒扫；选区先滤 `disabledSessionIds` 再截断，被滤数进 `skippedOff`）；`/memory session [on｜off｜status]`（短码显示、切换落 `session-switch` 审计行、`text: null`）；`GET/POST /api/memento/session`（**一条**路由按 method 分派——注册表以 path 为键，同 path 两条会撞 duplicate route；POST 只收 `{sessionId, enabled}`，多余字段与非法 id → 400）。**⑥`lib/strings.mjs`**：`COMMAND_TEXT`（含 `CommandTextBundle` typedef）从 `index.mjs` 整体搬入，新增 8 条会话开关文案（en/zh）并补齐 `usage` / `unknownVerb` / 帮助行。**⑦`client/client.js`**：`conversation.composer.dock`（session scope）注册开关钮（`SWITCH_STRINGS` en/zh；读 GET、写 POST、就地刷新；拿不到 `sessionId` 或注册被拒时降级只读显示，绝不改宿主）。**⑧`test/session-switch.test.mjs`（新，11 条）**。**⑨文档**：CHANGELOG、ARCHITECTURE 决策 13、AGENTS 模块表与 schema 版本、五语 README（`/memory` 行 ＋ session switch 表面行 ＋ 会话开关要点）、本规格 3.4 现状回填。**门链 8/8**：`npm test` **284/284 全绿**（基线 273 ＋ 新增 11）；lint 0 errors；typecheck 0 错误；coverage `lib/strings.mjs` 100% / `lib/store.mjs` 97.96% / `lib/protocol.mjs` 95.24% / `index.mjs` 95.39% / all 96.25%（全过门；基线 all 96.17%）；`check:readmes` / `verify:self-c... (line truncated to 2000 chars)

**S3 口径与事实核对（防后续误读）**：

1. **问卷合法板块实为 24 个，不是 26 个。** 任务书写「七面 31 个子板块中剔除 5 个仅观察面」，但本清单第二节表格逐行实数为 **29** 个子板块（`docs/知识领域清单.md` 的「31」是**知识领域**数，不是子板块数——两者常被混引）。29 − 5 = **24**。skill 与题库按 24 落，并在正文里写明这处差异。
2. **`level` 有两个层级，别混用**：条目列 `entries.level` 是「知识与学科水平」类条目的辅助坐标；分领域刻度本身住在 `profile` 表（`domain × level × tier`），只能经 `memory_profile` 写。skill 的写入协议把这条分了工。
3. **profile 写的审批归属**：审批载荷形如 `[yammory_system] profile user/user-global [source:memory-profile-tool]`，粒度策略表里 `user/user-global` 与 `source:memory-profile-tool` 两个键都对它生效；首次写入载荷给目标档位，改档给 `旧 → 新`。
4. **skill 的模型可见性**：本 skill 未设 `disable-model-invocation`——若硬禁用模型调用，「用户打字说『做画像问卷』」这条最自然的触发路径会失效。约束改写在正文首节：「只由用户主动发起，模型不得自作主张开问卷」。
5. **`lib/gate.mjs` 的正则放宽**（`parseWriteReason` 的 action 段由 `[a-z]+` 放宽为 `[a-z][a-z-]*`）是工作区里发现的**并行改动**，不是本轮所写；它与本轮实现兼容（本轮 profile 审批动作词取纯字母 `profile`），予以保留并在此登记。
6. **问卷 skill 的设计口径（0913 改版后）**：一轮至多 7 题、七题一停；挑板块「两端优先」；含糊以「每题 4 要点、答清 ≥70%」为准、追问上限一次；知识水平「抬头半档问」；重问按半衰期 ＋ 事件、不按日历；与观察冲突走**分面裁决**（能力听观察 / 意愿听自陈 / 落差两条都留）。**这条会牵动 S5 的仲裁逻辑——S5 实现时以本节为准，别再照旧的「一律观察优先」做。**

### 已知未闭合项（S2 / S3 留账，别当已完成）

| # | 事项 | 现状 | 影响 |
|---|---|---|---|
| 1 | **工作区/agent 轨条目失去自动可见性** | 预热段按规格只放 user-global；`memory_recall` 需要模型先「想到去查」 | 项目约定、环境教训可能整场不被想起。缓解：预热头已写明「按需取」，工具描述已写明分路。真正的解法（如常驻一行目录 / 会话首轮提示 / 情境触发）留给 S4 观察通道 |
| 2 | **表达约束段的 token 成本** | 每次会话固定注入四档规则表（中文约 350 字，含 1 个领域时约 300 字） | 规格 4.1 要求每档一句「说话要求」，照做即此成本。若嫌贵，可只在场当前档 + 默认档一句 |
| 3 | **四档 → `profile.tier` 用词** | 枚举取 `KNOWLEDGE_TIERS` 的「硕士」；清单含义栏写作「硕士 / 资深」 | 已在上文 4.1 加落库口径说明，测试断言两表逐值一致 |
| 4 | ~~typecheck 6 条既存错误~~ **已闭合（0913-17）** | 修完 8 条报错（5 处根因，全在注解面）：`index.mjs:74`、`lib/store.mjs:112/193-196/208/628-631/645/589`、`lib/protocol.mjs:111` | `npm run typecheck` **0 错误**；`npm test` **207/207 全绿**；零运行期语句变更 |
| 5 | **skill 未进 npm 包** | `package.json` 的 `files` 白名单未含 `skills/`，npm 安装的用户拿不到 skill 源 | 本轮按「源文件纳入 git」交付，从仓库取；是否随包发布留待分发口径统一时决定（加 `files` 一条即可） |
| 6 | **用户级 skill 安装未由助手执行** | 写入 `~/.dsh/skills` 属工作区外操作，本轮提权申请被用户拒绝；改以**项目级** `<repo>/.dsh/skills`（DSH 本地发现 rank 100）完成实测：新目录被 watcher 捕获，本会话模型侧目录即时出现 `yammory-survey`，`skill` 工具可完整加载正文 | 用户级安装命令（复制 / 目录联接两种）已写入 `skills/README.md`；同名时项目级 rank 100 胜出，两级可共存且指向同一份源 |
| 7 | **问卷题库未经真人试跑** | 24 板块的题面、「要点」标注与选项映射是设计稿，尚未在真实用户身上验证「一轮至多 7 题」的时长与流失率 | 属 S4/S5 回访时一并校准；题目文本全部集中在 `references/question-bank.md`，改题不动代码 |
| 8 | **三数的面板 UI 行未渲染（F7 留账）** | 三数的**数据面**已就绪（`GET /api/memento/stats` 返回 `stats` ＋ 渲染好的 `lines`），但 `client/client.js` 里那一行没加——`client/client.js` 不在 `docs/F6F7施工方案.md` §3 的点名文件内，按禁区纪律没动它 | 面板抽屉里暂时看不到三个数；`/memory stats` 与路由都可用。补这一行的成本很低（读该路由、在抽屉里多渲染 `lines`），等分发口径允许动 client 时一并做 |
| 9 | **整理机 v1 的触发只提示、不自动跑** | 这是**规格 3.5.1 的 v1 裁剪**（方案 §1.3 明确），不是缺陷：`agent/turn-stopping` 只读算积压，过线落一行 `tidy-due` ＋ 下个会话预热段末行提示；真整理必须由用户开口或 `/memory tidy` 显式发起 | 长期只写不整理时，重复条目会攒着；但自动跑会引入会话日志外的判断（撞「模型可见 ⟺ 落盘」），v2 若要做，得先解决「整理决策如何落盘」这一条 |

---

*本文件为弹性草稿，中途变直接改本文件。*
